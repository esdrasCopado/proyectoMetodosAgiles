const config = {
    application: {
        cors: {
            origin: ["http://localhost:5500"], // Cambia el puerto si es necesario
            credentials: true
        }
    }
};

export default config;
